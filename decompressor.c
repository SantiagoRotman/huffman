#include <stdlib.h>
#include <stdio.h>
#include <string.h>
#include "io.c"

int decompresor(char * encodedTextPath, char * encodedTreePath) {
    int encodedTextLen = 0, encodedTreeLen = 0;

    char *encodedFileText = readfile(path, &encodedTextLen);
    char *encodedFileText = readfile(path, &encodedTextLen);

}